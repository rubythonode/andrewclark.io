export default async function renderAppToString() {
  // TODO: add server-side rendering once Relay supports it. Until then, we'll
  // just return an empty string
  // https://github.com/facebook/relay/issues/136
  return '';
}
